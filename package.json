{
  "name": "kashkool-wrangler-gate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "node --check src/index.mjs",
    "type-check": "node --check src/index.mjs",
    "test": "node --test src/index.test.mjs"
  }
}
